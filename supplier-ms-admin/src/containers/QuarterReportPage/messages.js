/*
 * QuarterReportPage Messages
 *
 * This contains all the text for the QuarterReportPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.QuarterReportPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the QuarterReportPage container!',
  },
});
