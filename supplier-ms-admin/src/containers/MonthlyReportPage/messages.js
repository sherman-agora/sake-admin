/*
 * MonthlyReportPage Messages
 *
 * This contains all the text for the MonthlyReportPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MonthlyReportPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MonthlyReportPage container!',
  },
});
