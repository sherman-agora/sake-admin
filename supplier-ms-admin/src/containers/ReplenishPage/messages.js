/*
 * ReplenishPage Messages
 *
 * This contains all the text for the ReplenishPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReplenishPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Replenish',
  },
});
